import type { Services } from '../services';
import type { NormalizedHeader } from './normalize';
export declare function validateHeader(
    h: NormalizedHeader,
    _sv: Services
): {
    header: NormalizedHeader;
    warnings: string[];
};
//# sourceMappingURL=validate.d.ts.map
